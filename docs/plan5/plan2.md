## prompt

consolidate review below with yours, also are the Beads ideas already integrated to Conductor? if not sure, just tell me and we will let the coding agent investigate, the goal here is to nail down a solid integration plan with Pizza Bot with Conductor, Solvent and EBP 2.1 in one cohesive coherent methodology as POC for other domains, we should prove that our workflow is domain-agnostic.




Yes. I would consolidate the review, but **subtract two things from it** before implementation:

1. Do **not** create one Pizza Bot Skill per EBP debt type yet.
2. Do **not** build an EBP cascade mechanism yet.

Both are plausible later; neither is required to prove the POC.

On Beads: **some ideas are already present in Conductor, some are not confirmed, and I would have the coding agent inspect the current Conductor before changing anything.** From the current architecture we know `claim/release` and dependency semantics already exist; the stronger Beads-style `READY` frontier and provenance relationships such as `discovered-from` have not been established as locked Conductor features. So I would not assume either way.

## 1. Target POC Architecture

```text
Human
  ↓
Pizza Bot Agent
  ├── PLAN / WORK
  ├── EBP v2.1 grammar
  ├── tools / MCP
  └── subagents if the Agent chooses
          │
          ├──────────→ EBP Ledger
          │             Ideas / Debt / Promotion
          │
          └──────────→ Conductor
                        Project state / work / history
                              │
                              ↓
                           Solvent
                              ↓
                           Executor
                              ↓
                         External SOR
```

The important separation is:

```text
EBP        = epistemic grammar
Conductor  = coordination/state
Solvent    = authority
Executor   = effect
Pizza Bot  = Agent
```

That is domain-agnostic.

---

# 2. EBP Should Be a Methodology Layer, Not Conductor Logic

The attached EBP specification is deliberately tool-agnostic and minimal: `capture`, `status`, `retire`, `add-debt`, `promote`; promotion is derived from current debt rather than stored as a stale flag.  

Therefore:

**Do not add EBP semantics to Conductor core.**

Instead:

```text
Pizza Bot
   ↓
EBP MCP
   ↓
EBP ledger
```

Conductor only coordinates work generated by EBP.

---

# 3. Minimal EBP POC

Use the five canonical operations:

```text
capture
status
retire
add-debt
promote
```

Nothing else initially.

The EBP store should preserve the minimal Lean-shaped Idea:

```text
owner
claim
source
born
debt
containsFinalTruthClaim
```

with:

```text
promoted := debt empty ∧ no final-truth claim
```

rather than storing a mutable `promoted=true`. 

### Do not add yet

```text
dependency graph
cascade engine
review hierarchy
scoring
priority
dashboard
formal Lean service
```

---

# 4. EBP ↔ Conductor Mapping

This is the key integration seam.

```text
EBP Idea
   ↔
Conductor project artifact/reference

EBP Debt
   ↔
Conductor work item

Debt retirement
   ↔
completed Conductor work + evidence

Obstruction
   ↔
Conductor work/history

Promotion
   ↔
accepted/promoted research artifact
```

But Conductor does **not** decide whether the debt is actually retired.

The Agent/EBP layer produces the substantive result; Conductor coordinates the work.

---

# 5. Cross-Idea Dependency: Explicitly Defer It

The review correctly identifies a genuine gap:

```text
T1 changes
  ↓
T2 depended on T1
```

EBP v2.1 has no native dependency propagation.

Do **not** add a `belief_edge` / cascade mechanism now.

For the pilot:

```text
EBP detects/records change
        ↓
Agent/Skill recognizes consequence
        ↓
creates Conductor re-review task
        ↓
Conductor dependency/READY state handles coordination
```

That is sufficient.

If a real BM-IST case demonstrates that this cannot be managed without semantic propagation, that becomes a Growth Gate.

---

# 6. Pizza Bot Integration

Pizza Bot is the right Agent host because it already has a stateful DeepAgents/LangGraph runtime, skills, MCP extension, subagent delegation, and human-in-the-loop facilities.   

Create **one** skill:

```text
ebp-bmist
```

Not:

```text
map-builder
toy-checker
null-model-hunter
...
```

yet.

The single skill teaches the Agent:

```text
EBP vocabulary
Conductor workflow
how to create/retire debt work
when Human approval is required
when Solvent is required
```

The Agent may internally choose tools/subagents.

---

# 7. Faithfulness Review

Keep this explicitly Human-controlled.

EBP says contested faithfulness blocks promotion and requires an explicit mismatch reason. 

So:

```text
Agent proposes faithfulness result
        ↓
Pizza Bot Action
        ↓
Human approves/rejects
        ↓
ebp_retire needFaithfulnessReview
```

No autonomous faithfulness promotion.

Pizza Bot's existing Action queue is a natural fit for this; we should use it rather than build another review mechanism. Its workflow already includes durable approval requests and an Action queue. 

---

# 8. Conductor's Role

Conductor manages:

```text
Task:
    retire needMap
    retire needInvariant
    run toy check
    investigate obstruction
    perform faithfulness review
```

It manages:

```text
dependencies
claims
READY
activity
current state
history
```

It does **not** understand what `needMap` means.

That remains EBP semantics.

This is how we stay domain-agnostic.

---

# 9. Beads: What to Keep

The Beads-inspired ideas worth investigating are:

```text
READY frontier
atomic claim
parent/child
discovered-from
```

The strongest is `READY`: derive the currently claimable frontier from open work + satisfied dependencies + no claim. Beads explicitly uses that model. 

### Current Conductor status

From the architecture we already know:

```text
atomic claim       = already present
dependencies       = already present
lifecycle          = already present
activity/history   = already present
```

I am **not sufficiently certain** that these are already present as first-class semantics:

```text
derived READY frontier
parent/child
discovered-from
```

So don't modify Conductor yet.

Have the coding agent inspect the repository and produce:

```text
BEADS_CONDUCTOR_GAP.md

feature
current Conductor equivalent
status:
    PRESENT
    PARTIAL
    MISSING
recommendation
```

No code changes during this audit.

---

# 10. BM-IST First Claim

I agree with the review's strategy of taking **one real idea through the entire system**, rather than loading the entire corpus into the POC immediately.

Use:

```text
Theorem / Claim 1:
linear Madelung ⇒ Fisher structure
```

as the first candidate **provided the existing BM-IST artifacts confirm it is sufficiently characterized to serve as a real pilot case**.

The workflow becomes:

```text
EBP capture
    ↓
debt attached
    ↓
Conductor work created
    ↓
Pizza Bot Agent works debt
    ↓
Human faithfulness gate
    ↓
EBP promotion
    ↓
promoted artifact
```

This is a much better test than building infrastructure first.

---

# 11. Consequential Moment

Once the research artifact is promoted, deliberately perform **one external consequential operation**:

```text
publish/promote artifact
        ↓
Solvent
        ↓
Executor
        ↓
GitHub SOR
```

This reuses the real Phase 2 path we already proved.

The synthesis itself remains ordinary intellectual work.

Only the external effect crosses Solvent.

---

# 12. Domain-Agnostic Test

The BM-IST pilot should prove that **EBP is replaceable and Conductor is oblivious to the domain**.

After the first claim works, conceptually substitute:

```text
BM-IST
```

with:

```text
another research domain
```

The Conductor protocol should remain unchanged:

```text
project
task
dependency
claim
READY
submit
history
```

Only the Agent Skill + EBP content changes.

That is the real POC.

---

# 13. Implementation Sequence

```text
0. Audit Conductor against Beads semantics
   ↓
1. Implement minimal EBP ledger/MCP
   ↓
2. Create one Pizza Bot `ebp-bmist` Skill
   ↓
3. Connect Pizza Bot → Conductor MCP
   ↓
4. Connect Pizza Bot → EBP MCP
   ↓
5. Seed one BM-IST claim
   ↓
6. PLAN
   ↓
7. Human approval
   ↓
8. WORK through Conductor READY/claim
   ↓
9. Retire EBP debt
   ↓
10. Human faithfulness gate
   ↓
11. Promote
   ↓
12. One consequential publication
   ↓
13. Solvent → Executor → GitHub SOR
```

## 14. Hard Stop Rules

No:

```text
Conductor redesign
EBP cascade
new Solvent capability
new Executor
Pizza Bot runtime modification
multiple specialized Skills
domain-specific Conductor fields
```

unless the pilot produces evidence requiring them.

### The resulting methodology

```text
Pizza Bot
    = Agent

EBP 2.1
    = research grammar

Conductor
    = durable coordination/control plane

Solvent
    = consequential authority

Executor
    = external effect

BM-IST
    = first domain workload
```

That is the cohesive POC I would build. The **Beads audit should be the very first coding-agent task**, because we should verify exactly what Conductor already has before adding even the smallest READY/provenance changes.



## prompt

give me what are lacking now that needs to be implemented and are absolutely necessary before we even begin, short but concise only in itemized category format



## Before the BM-IST POC, only these are essential

### 1. Conductor audit — **mandatory first**

Verify what Conductor already has versus Beads-inspired needs:

* derived `READY` frontier
* atomic claim
* parent/child provenance
* `discovered-from`
* durable project state/history

**Do not modify Conductor during the audit.**

### 2. Minimal EBP 2.1 store — **mandatory**

Implement only:

```text
capture
status
retire
add-debt
promote
```

with EBP's minimal `Idea` + `DebtItem` semantics.

No EBP cascade engine.

### 3. EBP ↔ Conductor mapping — **mandatory**

Define exactly:

```text
EBP Idea
→ project artifact/reference

EBP Debt
→ Conductor work

Debt retirement
→ work + evidence

Promotion
→ accepted research artifact
```

No EBP semantics inside Conductor.

### 4. Pizza Bot `ebp-bmist` Skill — **mandatory**

One Skill only.

It teaches the Agent:

* EBP vocabulary
* how to use EBP MCP
* how to use Conductor MCP
* PLAN → human approval → WORK
* when Solvent is required

Do not create one Skill per debt type yet.

### 5. Pizza Bot ↔ Conductor MCP — **mandatory**

The Agent must be able to:

```text
read project state
discover READY work
claim
update
submit
read history
```

### 6. Pizza Bot ↔ EBP MCP — **mandatory**

The Agent must be able to perform the five EBP operations through MCP.

### 7. Human faithfulness gate — **mandatory**

Use Pizza Bot's existing Action/HITL mechanism.

```text
Agent proposes faithfulness result
→ Human decides
→ EBP debt retired or remains
```

No new approval subsystem.

### 8. One consequential-action path — **mandatory**

Reuse the already-proven:

```text
Pizza Bot
→ Solvent
→ existing Executor
→ GitHub SOR
```

No new Executor or Solvent work.

### 9. BM-IST seed state — **mandatory**

Seed **one** real BM-IST Idea/Claim and its current EBP debt.

Do not ingest the entire corpus into Conductor.

### 10. Stop here

Everything else is **post-POC**:

```text
EBP dependency/cascade
specialized Skills
claim TTL
scheduler
knowledge graph
new Conductor features
new Solvent features
domain-specific extensions
```

The POC should prove only this:

> **One generic Agent can use EBP 2.1 to perform research work, Conductor can durably coordinate that work, and Solvent can safely gate the rare consequential external action—without any component becoming another's job.**
